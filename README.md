# Wi-Fi Fingerprinting Indoor Localisation
Indoor positioning via Wi-Fi RSSI fingerprinting without GPS.
### ■ Research Highlights
- **Fingerprint Database**: Collects RSS vectors at labelled grid points and stores them as a spatial fingerp- **Probabilistic Localisation**: Uses Gaussian likelihood models per AP to compute a posterior position est- **Algorithms Focus**: Evaluates mean localisation error vs. grid density and number of APs, guiding deploym**Tech Stack:** Python, `NumPy`, `scikit-learn`.
